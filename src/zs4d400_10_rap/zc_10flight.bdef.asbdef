projection implementation in class ZBP_C_10FLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_10FLIGHT alias Zc10flight
extensible
use etag
{

field (readonly) planetypeID;
  //use create;
  use update;
  //use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}